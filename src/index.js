import React from 'react';
import ReactDOM from 'react-dom';
import RTChatContainer from './RTChatContainer';
import './index.css';

ReactDOM.render(
  <RTChatContainer className="rtchat-container" />,
  document.getElementById('root')
);
